{% extends 'base.html.twig' %}

{% block title %}Troubleshooting{% endblock %}

{% block body %}
<div class="container py-5 text-center">

  <h2 class="mb-4 fw-bold">What Issue Is Your Client Experiencing?</h2>
  <p class="text-muted mb-5">Select the correct product below & start troubleshooting </p>

  <div class="d-flex flex-wrap justify-content-center gap-3">
    <a href="{{ path('troubleshooting_product', { product: 'airmobile' }) }}" class="btn btn-outline-danger px-4 py-2">AirMobile</a>
    <a href="{{ path('troubleshooting_product', { product: 'wireless' }) }}" class="btn btn-outline-danger px-4 py-2">Wireless</a>
    <a href="{{ path('troubleshooting_product', { product: 'dsl' }) }}" class="btn btn-outline-danger px-4 py-2">DSL</a>
    <a href="{{ path('troubleshooting_product', { product: 'fibre-orders' }) }}" class="btn btn-outline-danger px-4 py-2">Fibre (Orders)</a>
    <a href="{{ path('troubleshooting_product', { product: 'fibre-technical' }) }}" class="btn btn-outline-danger px-4 py-2">Fibre (Technical)</a>
    <a href="{{ path('troubleshooting_product', { product: 'voip' }) }}" class="btn btn-outline-danger px-4 py-2">VoIP</a>
    <a href="{{ path('troubleshooting_product', { product: 'hosting' }) }}" class="btn btn-outline-danger px-4 py-2">Hosting</a>
    <a href="{{ path('troubleshooting_product', { product: 'accounts' }) }}" class="btn btn-outline-danger px-4 py-2">Accounts</a>

  </div>

</div>

{% endblock %}
