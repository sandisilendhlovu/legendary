{% extends 'base.html.twig' %}

{% block title %}Welcome{% endblock %}

{% block body %}
<div style="max-width: 600px; margin: 80px auto; text-align: center;">
      <h1>👋 Welcome back, {{ app.user.firstName ?? app.user.email }}!</h1>
      <p>You’re now logged into Legendary 🎉</p>

      <a href="{{ path('app_logout') }}" 
         style="display:inline-block; margin-top: 20px; padding: 10px 20px; background:#e60000; color:white; border-radius:8px; text-decoration:none;">
         Logout
      </a>
  </div>
{% endblock %}
