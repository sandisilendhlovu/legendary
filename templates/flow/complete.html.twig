{% extends 'base.html.twig' %}

{% block title %}Flow Complete{% endblock %}

{% block body %}
<div class="container py-5 text-center">
  <h2>{{ flow.title }} completed! </h2>
  <p class="text-muted">You’ve reached the end of this troubleshooting flow.</p>

  <a href="{{ path('flow_view', { id: flow.id }) }}" class="btn btn-danger mt-3">
    Restart Flow
  </a>
</div>
{% endblock %}
